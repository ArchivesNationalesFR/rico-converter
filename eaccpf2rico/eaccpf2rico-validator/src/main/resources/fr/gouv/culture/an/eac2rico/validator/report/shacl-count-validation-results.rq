PREFIX shacl:   <http://www.w3.org/ns/shacl#>

SELECT ?severity (COUNT(?s) AS ?count)
WHERE {
	?s a shacl:ValidationResult .
	?s shacl:resultSeverity ?resultSeverity.
	BIND(STRAFTER(STR(?resultSeverity), "#") AS ?severity)
}
VALUES
GROUP BY ?severity